# Foundations audit, 2026-09-08

Scope: `src/lib/constants.ts`, `src/app/globals.css`, `src/lib/fonts.ts`, `docs/DESIGN.md`,
`docs/BUILD_PLAN.md`, checked against `docs/BRIEF.md` (facts), `docs/PHOTOS.md` and
`public/photos` (assets), `src/lib/reviews.ts` and `docs/REVIEWS.json` (reviews), and the frozen
infrastructure (`asset.ts`, `seo.ts`, `next.config.ts`).

## 1. TypeScript

- `npx tsc --noEmit -p tsconfig.json` exits 0, before and after the edits below. No errors in
  `constants.ts` or `fonts.ts`. (No pages exist yet, so there is nothing else to fail.)
- `fonts.ts`: `Bricolage_Grotesque` with `weight: "variable"` and `axes: ["opsz", "wdth"]` and
  `IBM_Plex_Mono` with weights 400 and 500 both type-check against this repo's `next/font/google`
  data, so the axes DESIGN.md 2.3 relies on will be served.
- A bundled runtime load of `constants.ts` (esbuild, `@` alias) confirms: `WORK.length` 60,
  `CITIES.length` 12, `SERVICES.length` 6, gallery groups 49 / 3 / 2 / 2 / 1 / 1 / 2, and the
  derived review line resolves to "4.6 from 68 reviews, as of September 8, 2026".

## 2. Dashes and characters

- U+2014 and U+2013: zero hits in all five files, before and after.
- Non-ASCII: zero characters in all five files.
- Exclamation marks: none in copy. The single `!` in `constants.ts` is the logical not in
  `photo()`. (`reviews.ts` carries `!` and a curly apostrophe inside verbatim Google reviews;
  the brief requires reviews verbatim, so those stay and the file is frozen anyway.)

## 3. Fact audit (constants.ts against BRIEF.md)

Checked and matching exactly: phone (248) 259-1617 with `tel:+12482591617` and `sms:+12482591617`;
email rickwraps101@gmail.com and no other; address 13417 E Eight Mile Rd, Warren, MI 48089; lat
42.4497, lng -82.9877; hours Monday 12 to 7 pm, Tuesday to Saturday 10 am to 6 pm, Sunday closed,
plus the 24 hour `hoursSchema` (12:00 to 19:00, 10:00 to 18:00); "by appointment" everywhere;
rating 4.6 from 68 as of September 8, 2026 with the cid listing URL and Place ID; Instagram,
TikTok and Facebook URLs and handles; Square booking URL; SITE_URL https://rickywrapsllc.com with
the hijacked rickywraps.com never linked; brand name, byline, legal name, alternateName, owner
"Carlton Spencer, known as Ricky"; films "Avery Dennison and 3M films, XPEL paint protection
film" in the products sense only; service area, twelve cities and three counties with the
county assignments BUILD_PLAN expects; tint tiers (Standard 1 year; Black carbon about 60
percent heat, 99 percent UV, 3 year; Ceramic about 80 percent heat, 5 year; "Ask" where the
brief has no figure; "Quoted per vehicle" in every price cell); full wrap 1 to 3 days; powder
coated wheels 1 to 2 days; PPF self healing and "long term" (no 10 years); mobile tint "available
by appointment, ask when you book" with no days; wash and detail add-ons in one line; no Royal
Oak location claimed; no SEMA build; no prices ("$" followed by a digit: zero hits); no years in
business, counts, guarantees, "family owned" or "award".

FAQ answers were traced to `docs/OLD_SITE_TEXT.txt` (hand wash with mild soap, no brush washes,
check edges; DIY kits versus professional tools; self healing "with heat from the sun or a warm
garage"; paint-safety sentence) and the old numbers (7 to 10 years, 30 percent energy, 15 year
warranty) are absent.

"The shop you know as Ricky Wraps": exactly once in the home hero sub and once in the About lede
(the third hit is the `BRAND.knownAs` constant itself).

Photos: all 61 files in `public/photos` are referenced (60 `WORK` entries plus the timelapse
poster), no reference points at a missing file, and every width and height matches PHOTOS.md and
the files on disk (spot-checked with sips). Every `photoId`, `coverPhotoId`, `chipPhotoId`,
`pairIds`, `RECENT_WORK`, `GALLERY_ONLY`, `ABOUT.photoIds` and `SHADE_SCENE_ID` resolves to a
`WORK` id. All 60 entries agree with the DESIGN.md section 8 table on chip hex, label, setting,
service / finish / colour tags and object-position. No gallery-only photo is placed on a content
page. No label or alt says "outside the shop", "in front of the shop" or "Eight Mile".

### Fixed in constants.ts

1. `BRAND.addOns` said add-ons are available "with any job". The brief only supports "wash and
   detail add-ons" as a one-line mention; "with any job" was an invented claim. Now: "Wash and
   detail add-ons are available, ask when you book."
2. The Google line "4.6 from 68 reviews, as of September 8, 2026" was typed three times (HERO
   facts, SPEC, REVIEW_SUMMARY) while `reviews.ts` is regenerated by a script, so a re-run would
   have left the hero and shop sheet stale. Added `REVIEW_LINE`, built from `REVIEWS.rating`,
   `REVIEWS.count` and `REVIEWS.asOf`, and pointed all three at it. `REVIEW_SUMMARY` now reads
   its numbers from `REVIEWS` too.
3. `BUSINESS_DESCRIPTION` (JSON-LD) said "the shop known as Ricky Wraps" and `SEO.about.description`
   said "the shop you know as Ricky Wraps", which would have put the phrase in a third place
   (search snippets). Both now read "One Stop Customs, by Ricky Wraps".
4. `FORM.thankYouPath` was "/thank-you" without the trailing slash the static export requires;
   now "/thank-you/".
5. The Roseville city note cited I-696 as a route to Eight Mile, which is loose (I-696 runs
   parallel, not to it). Rewritten as plain geography: Roseville is northeast of Warren, Gratiot
   runs southwest to Eight Mile, the shop is west along the road.

## 4. Consistency (tokens, classes, exports)

Tokens: every token DESIGN.md 2.1 names (paper, white, liner, ink, graphite, black, ash, signal,
error, the two font variables, hairline-on-black, hairline-on-black-strong, ease-out, nav-h,
bar-h, chip-h, strip-h, shade) exists in globals.css with the same value. globals.css also defines
`--ink-hover`, `--ink-underline` and `--breakpoint-xs`, which DESIGN.md did not list; added them to
2.1.

Classes: every class DESIGN.md and BUILD_PLAN.md promise exists in globals.css, with one
exception: BUILD_PLAN told lane C to build the phone-only finish row from a `.strip-row` class
that does not exist; globals.css has `.snap-row-sm` for exactly that, so the plan now names it.
Nineteen classes globals.css defines were documented nowhere (`.bar-cell`, `.card-link`,
`.faq-answer`, `.header`, `.menu`, `.menu-row`, `.menu-sheet`, `.nav-link`, `.only-lg`,
`.under-lg`, `.pane-photo`, `.process`, `.rows-tint`, `.sheet-row`, `.snap-row`, `.snap-row-sm`,
`.strip-compact`, `.ticket-section`, `.tiers-note`). Added DESIGN.md 2.6 "Class inventory" listing
every class with its job, and replaced the BUILD_PLAN phase 0 class list with the full set.

`.t-lede`: DESIGN.md's type table said graphite, DESIGN.md 5.15 said a review quote is a lede in
ink, and globals.css resolves this by giving `.t-lede` no colour (ledes add `.muted`). The table
now says so.

Spelling: DESIGN.md and BRIEF.md are written in British spelling (colour, grey) and quote copy that
way; constants.ts uses American spelling for customer-facing copy (color, gray), documented in its
header, while keeping `colour` as the code key. BUILD_PLAN's Timelapse acceptance would have
failed on "Satin, grey". Added the spelling rule to DESIGN.md 6 and BUILD_PLAN shared contract 12
and fixed the Timelapse acceptance string. Constants is authoritative for exact strings.

Exports: every constant BUILD_PLAN.md and DESIGN.md section 8 name exists in constants.ts
(`SITE_URL`, `BASE_TITLE`, `HOME_TITLE`, `BRAND`, `DOORS`, `NAV_LINKS`, `MENU_LINKS`,
`FOOTER_LINKS`, `CREDIT`, `HERO`, `WORK`, `WORK_BY_ID`, `photo`, `TIMELAPSE`, `FINISHES`,
`FINISH_NOTE`, `RECENT_WORK`, `HOME_SECTIONS`, `PPF_ROWS`, `FLEET_ROWS`, `POWDER_ROWS`,
`WRAP_TYPES`, `OTHER_WRAPS`, `BUILDING_ROWS`, `TINT_TIERS`, `SHADES`, `SHADE_LEGAL`, `TINT_ROWS`,
`PROCESS`, `TIME_LEDGER`, `SERVICE_PAGES`, `CITIES`, `CITY_COPY`, `QUOTE_OPTIONS`, `FORM`,
`ABOUT`, `CONTACT`, `THANK_YOU`, `NOT_FOUND`, `REVIEWS`). BUILD_PLAN fixes made:

- It called `WORK_BY_ID("id")` as a function in five places; `WORK_BY_ID` is a record and
  `photo(id)` is the function. All five now read `photo("id")`, and shared contract 6 names both.
- It had lane A declare the shared types in `src/lib/types.ts` while constants.ts already declares
  and exports them, which would have produced two declarations of every shape. `types.ts` is now
  a re-export file and the contract says never redeclare.
- `meta.ts` was to define `HOME_TITLE`, which constants.ts already exports; it now re-exports it
  and builds `titleFor` from `BASE_TITLE`.
- The sitemap acceptance said 23 URLs; the indexable route count is 22 (thank-you is noindex and
  must stay out of the sitemap). Corrected with the list.
- Reviews acceptance now names `REVIEWS.asOf` and `REVIEW_LINE` so the three placements agree.

DESIGN.md 7.2 said every service lede ends "Quoted per vehicle, by appointment, at 13417 E Eight
Mile Rd in Warren"; the buildings page in constants correctly says "per job" since it quotes
glass, not vehicles. Recorded as the one exception.

Checked and left alone: `.chip-label` uses `min-height: 30px; padding: 4px 12px` where DESIGN.md
5.4 says height 30px and padding 0 12px; the difference only shows when the left label wraps,
which DESIGN.md allows ("the strip grows"), so the CSS is the better contract. The page-load gate
attributes (`data-js`, `data-motion`) match between DESIGN.md 3.1 and globals.css. `SHADE_SLIDER`
(0 to 92, step 4, value 60), `SHADES` opacities, `DOORS` order and labels, `FOOTER_LINKS`,
`MENU_LINKS`, `GALLERY_ONLY` (26 ids) and the city cover rotation all match DESIGN.md.

## 5. Unresolved (not mine to decide)

- `src/lib/reviews.ts` holds five items including "Fadi A." (November 2025) that is not in
  `docs/REVIEWS.json`. The file is frozen and regenerated by `scripts/fetch-reviews.mjs`, which
  needs `GOOGLE_PLACES_API_KEY`; the key is not in this environment, so the script was not run.
  Re-run it before launch so the file and the listing agree. The derived `REVIEW_LINE` means the
  hero and shop sheet will follow whatever the script writes.
- Chip hexes for the black, white and grey cars were set by hand (DESIGN.md 8); every chip still
  needs the by-eye check in the built site.
- City notes contain real geography (which road, which side of Eight Mile, county line) and no
  drive times. They go a little beyond the brief's facts (the brief only names the cities and
  counties). They are verifiable statements, not claims about the shop, and DESIGN.md 7.3 asks
  for exactly this, but Nick may prefer to cut the route sentences to the county alone.
- Gates from the brief remain open and are recorded, not decided: Web3Forms key, domain cutover,
  the current shop address and the Royal Oak question, tint warranty terms, stocked shades, the
  three readable plates, which building is the current shop.
- LCP with the peel in place must be measured on the preview, per DESIGN.md 3.1.

## 6. Files touched

- `src/lib/constants.ts` (five fixes above; `REVIEW_LINE` added as a new export)
- `docs/DESIGN.md` (2.1 derived values, 2.3 `.t-lede`, new 2.6 class inventory, 5.15, 6 spelling
  rule and `REVIEW_LINE`, 7.2 buildings exception)
- `docs/BUILD_PLAN.md` (phase 0 class list, types contract, meta.ts, `photo()` calls, shared
  contracts 6 and 12, FinishRow class, sitemap count, Timelapse and Reviews acceptance)
- `src/app/globals.css` and `src/lib/fonts.ts`: no changes needed.
